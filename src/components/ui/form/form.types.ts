/**
 * Errores asociados a campos de un formulario.
 *
 * La clave corresponde al nombre o identificador del campo
 * y el valor contiene el mensaje que se mostrará al usuario.
 */
export type FormFieldErrors = Record<string, string>;
